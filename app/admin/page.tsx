import {session,isAdmin} from '@/lib/auth';import {db} from '@/lib/db';import {redirect} from 'next/navigation';
export default async function Admin(){const s=await session();if(!s||!isAdmin(s.id))redirect('/');const es=await db.election.findMany({include:{_count:{select:{ballots:true}}},orderBy:{createdAt:'desc'}});return <main className="wrap"><a href="/">← Home</a><div className="card"><h1>Create election</h1><form action="/api/admin/elections" method="post"><label>Title</label><input name="title" required/><label>Description</label><textarea name="description"/><label>Candidates</label><textarea name="candidates" required placeholder={'Alice\nBob\nCharlie'}/><small>One candidate per line.</small><br/><br/><label>Closing date/time</label><input name="closesAt" type="datetime-local" required/><button>Create election</button></form></div><div className="card"><h2>Elections</h2>{es.map(e=><p key={e.id}><b>{e.title}</b> — {e._count.ballots} ballots cast. Results remain hidden until closing time. <a href={`/election/${e.id}`}>Open</a></p>)}</div></main>}
